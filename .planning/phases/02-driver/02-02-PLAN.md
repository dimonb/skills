# PLAN — 02-02 · fill the driver body (next task)

**Requirements:** DRV-01, DRV-02, DRV-03. **Status:** planned, not started.

## Goal
Replace the `drv_*` stubs in `shared/driver/agent-driver.sh` with a real agterm|tmux driver that
is a **superset** of both `council/lib/term.sh` and `shipyard/shipyard-backend.sh` — able to
reproduce each skill's exact behaviour so migration (02-03/04) keeps tests green. No skill call
site changes in this task; the driver is implemented and unit-tested in isolation.

## Interface to implement (from CONTEXT decision 4)
- `drv_backend` — agterm when `agtermctl` answers, else tmux (merge `ct_backend` `term.sh:16-28`
  ↔ `shipyard_backend` `backend:32-54`).
- `drv_shq` — shell-quote (identical in both today).
- `drv_container` / `drv_container_pin` — **parameterised** naming: council appends `-ai`
  (`term.sh:53-62`), shipyard does not (`backend:129-168`). A flag/var selects the variant.
- `drv_target` — session/window lookup (`term.sh:67-78` ↔ `backend:222-235`).
- `drv_launch` — `zsh -lc 'exec <launcher>'`, `--wait --no-select`; tmux with the AGTERM_* scrub
  array (`term.sh:80-104` ↔ `backend:338-368`).
- `drv_tell` / `drv_submit` / `drv_read` / `drv_kill` / `drv_focus` — the remaining 1:1 pairs.
- `drv_signal` — minimal normalized read (liveness + capacity) sufficient to replace today's
  ad-hoc screen reads; full AgentSignal dispositions are Phase 3.

## Acceptance
- A new `shared/driver/tests/` (or a `t*.sh` beside each skill's suite) unit-tests the
  deterministic helpers: backend selection, `drv_shq` quoting, container-name shape for both
  variants, and the target-query construction — **without** a live terminal.
- `bash -n` clean; `scripts/sync-driver.sh` re-run; `make check` green (check 11 still passes).
- Nothing in council or shipyard calls `drv_*` yet, so both skills' behaviour is unchanged and
  their suites still pass.

## Then
- 02-03 migrate `council/lib/term.sh` to delegate to `drv_*`; council `tests/run-all.sh` green.
- 02-04 migrate `shipyard-backend.sh`; shipyard `tests/run-all.sh` green; confirm the ≥5 driver
  runs fine under shipyard (CONTEXT decision 2).
- 02-05 wire the affected suites into `make check` (closes C7).
