# Vendored copies of shared/policy/policy.sh — one path per line.
# `scripts/sync-driver.sh` writes each; `scripts/check.sh` (check 11) fails the gate on drift.
# Each plugin ships its own copy because a Codex plugin cannot depend on another plugin (the same
# reason shared/driver/ is vendored, not symlinked).
plugins/council/skills/council/lib/policy.sh
plugins/shipyard/skills/shipyard/policy.sh
